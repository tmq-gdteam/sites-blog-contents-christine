Legacy RPA clicks fixed pixels. Move a button two pixels and the bot falls over — a multi-million-dollar process down.

The shift that matters: agents that read meaning, not pixels. Understand the intent, survive the UI change.
